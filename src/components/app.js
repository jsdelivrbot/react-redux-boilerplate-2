import React, { Component } from 'react';

export default class App extends Component {
  render() {
    return (
      <div style={{marginTop: "20%", textAlign: "center", fontSize: "40px"}}>React Boilerplate with Redux</div>
    );
  }
}